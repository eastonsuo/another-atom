# Another Atom 整体产品目标与定位

[toc]

- 文档状态：整体产品设计基线
- 适用范围：V1、V2 及后续版本
- 产品概览：[README](../../../README.md)
- 参考资料：[Atoms 参考产品分析](./02-[参考]-Atoms参考产品分析.md)
- 版本关系：[V1 核心产品需求与交互](../V1/产品设计/01-核心产品需求与交互.md) · [V2 产品范围与交互](../V2/产品设计/01-产品范围与交互.md)

## 背景

README 适合说明产品概览，但不适合长期维护跨版本的定位取舍。V1 和 V2 需要共享一组稳定判断，避免版本升级时改变 Project 交付单位、平台控制权或用户对代码与发布的所有权。

## 摘要

- **交付单位**
  - Another Atom 以持续存在的 Project 为交付单位，不以单次回答或一次性代码片段作为完成结果。
- **交互入口**
  - 自然语言是主要入口，代码修改、版本和发布共同作用于同一 Project；Preview 只在项目类型和 Runtime 支持时出现。
- **Agent 价值**
  - 多 Agent 用于分担不同类型的不确定性，不以角色数量或表面自治程度作为产品目标。
- **平台与用户权利**
  - 身份、权限、配额、确定性校验和代码副作用由 Runtime 控制，代码、版本和发布指针保持可追溯并受用户控制。
- **版本演进**
  - V1 先完成可部署闭环，V2 再增加受控的自主多 Agent 协作。

## 1. 文档职责

README 已负责说明产品是什么、解决什么问题、提供哪些核心能力和整体架构，本文不再重复这些介绍。

本文只记录 README 不适合展开的整体产品决策：Another Atom 与 Atoms 的关系、关键选择的收益和代价、V1/V2 必须共同遵守的边界，以及判断产品是否成立的标准。版本内的功能范围、Agent 行为和工程实现分别以 V1/V2 文档为准。

## 2. 不随版本变化的定位边界

- **[交付单位]** 产品以持续存在的 Project 为交付单位，不以单次模型回答、一次性代码片段或静态截图为完成结果。
- **[交互入口]** 自然语言是主要入口，文件与版本管理作用于所有 Project；Preview 和视觉修改是网页等可预览项目类型的附加能力，不定义产品范围。
- **[Agent 价值]** 多智能体用于分担不同类型的不确定性；是否增加角色、并行或返工，由任务价值和证据决定，不以 Agent 数量作为产品目标。
- **[平台控制]** Agent 可以提出方案和修改，身份、权限、配额、确定性校验、代码写入和发布副作用由 Runtime 控制。
- **[用户所有权]** 代码、版本和发布指针必须可追溯并受用户控制；任何版本升级都不能破坏 Project 连续性或自动替用户上线。

## 3. 与 Atoms 的关系

根据公开参考资料，Atoms 的产品主链路是：

`输入需求与资料 → Agent 编排 → 预览和编辑 → 数据与外部服务 → 校验和修复 → 版本与发布 → 分享和增长`

Another Atom 保留这条产品主干，但采用自己的品牌、交互、Contract 和工程实现，不复制 Atoms 的界面、私有 Prompt 或未公开基础设施。

| 维度 | 共同目标 | Another Atom 的明确取向 |
| --- | --- | --- |
| 产品形态 | 从意图到在线产品的 Agent 工作台 | 默认以单一 Lead 作为用户入口，内部组织方式由系统负责 |
| 多智能体 | 专业角色协作完成复杂任务 | 角色必须交付可查看产物，避免退化成多个 Agent 互相聊天 |
| Vibe Coding | 自然语言驱动创建和修改 | 对话、视觉编辑和代码文件编辑共同服务同一个 Project |
| 项目工作区 | Chat、Viewer、Edit 和文件联动 | Project 从创建开始拥有代码仓库、版本和发布状态 |
| 质量与恢复 | 错误可检查、修复，历史可恢复 | 校验结果、失败原因、版本和恢复都作为持久事实保存 |
| 发布 | 版本可以发布、更新和下线 | Agent 不能自动发布；线上指针由用户明确控制 |

## 4. 关键产品取舍

以下内容记录 Another Atom 相对公开 Atoms 工作流做出的选择。它不代表 Atoms 没有对应内部实现；公开资料不足的地方不做推断。每项同时说明当前方向、收益、代价和仍未确定的边界。

### 4.1 一个 Lead，还是让用户先选模式和 Agent

- **当前倾向：** 默认只提供一个 Lead。Lead 判断是直接回答、简单修改还是组织团队，用户仍可查看实际调用的角色并覆盖关键判断。
- **优势：** 降低首次使用门槛，用户先表达目标，不需要先理解 Engineer、Team、Race 等内部模式。
- **代价：** 自动路由错误时可能让用户觉得失去控制，也会弱化高级用户主动选择执行成本和速度的能力。
- **待讨论：** 是否在高级设置中保留“快速实现 / 完整团队 / 多方案比较”等显式策略，但不把模式选择放在主入口之前。

### 4.2 固定审批，还是风险驱动确认

- **当前倾向：** 用户明确要求构建，已经授权一次受控工作；正常工作自动继续，只在范围适配、额外预算、破坏性代码操作和线上变化时确认。
- **优势：** 缩短 Golden Path，避免用户反复确认没有新增信息的步骤。
- **代价：** 用户失去每次构建前强制停下来编辑计划的窗口。
- **补偿方式：** 方案和中间产物始终可检查；用户可以暂停、修改、恢复或创建新版本。

### 4.3 代码仓库是后接能力，还是 Project 的默认组成

- **当前倾向：** 一个 Project 从创建开始就对应一个代码仓库；GitHub/GitLab 是未来可连接的 remote，而不是代码存在的前提。
- **优势：** 即使没有第三方授权，代码、版本和恢复也有稳定归属；平台版本能直接映射 commit。
- **代价：** 平台需要承担仓库持久化、隔离、备份和导出责任；接入 remote 后还需要处理双向同步和冲突。
- **待讨论：** 用户自有 remote 是自动镜像、手动 Push/Pull，还是以平台仓库为主、remote 只做导出。

### 4.4 Remix 解决上下文污染，还是在原 Project 内修复连续性

- **公开参考现象：** Atoms 建议连续多轮无法修复时，从当前版本 Remix 到新 Chat，以减少旧上下文干扰。
- **Another Atom 倾向：** 优先依赖结构化 Contract、版本和失败证据，在原 Project 中从明确基线继续；只有产品方向真正分叉时才 Remix。
- **优势：** 保留完整项目归属和版本历史，减少“为修复问题而复制项目”。
- **代价：** Runtime 必须正确处理 Context 失效、失败恢复和版本基线，工程要求更高。

### 4.5 先扩展功能广度，还是先证明项目闭环

- **当前倾向：** 先保证需求、生成、代码、校验、版本、发布和继续修改连成真实闭环，再增加 Cloud、支付、增长、作品广场和大量连接器。
- **优势：** 可以判断核心产品是否成立，不让外部服务数量掩盖生成和迭代体验的问题。
- **代价：** 短期功能丰富度会落后于 Atoms，能直接上线的应用类型也更受限制。
- **待讨论：** 闭环稳定后，优先补真实后端能力、代码协作，还是分享与 Remix 生态。

### 4.6 产品目标开放，还是 Runtime 无限制

- **公开依据：** Atoms 整体提供 [Web App、浏览器游戏、Presentation、Research 和 Coding Assistant 等不同入口](https://atoms.dev/usecases)；但其应用构建链路仍以 [App / Website](https://atoms.dev/usecases/app-and-website-builder) 为主要交付形态，[Atoms Cloud](https://atoms.dev/blog/introduce-atoms-cloud) 扩展的是带认证、数据库、支付和后端 API 的有状态 Web 应用。公开资料不能证明 Atoms 已支持任意原生 Runtime 或任意技术栈。
- **已确认结论：** Another Atom 接受任意软件产品目标，并保留用户指定的项目类型和目标平台。Preview 只是网页项目的能力；非 Web 项目不能为了套用现有 Preview 被改写成网页或商品目录。
- **为什么合理：** 产品目标、源码交付和 Runtime 执行是三个不同层次。Project 可以先保存需求、代码、Artifact 和版本，再根据项目类型选择构建、测试、预览或发布适配器。
- **优势：** 产品定位不会被当前 Web Renderer 反向限制；新增移动端、桌面端、CLI 或服务端适配器时，也不需要改变 Project、Agent 交接和版本模型。
- **代价：** 不同项目类型需要不同的源码 Contract、构建验证和运行环境，不能再用一个 Web AppSpec 和 iframe 代表所有交付完成。
- **适用边界：** 有匹配 Runtime 适配器的项目可以构建和运行；只有源码 Contract、没有运行适配器的项目仍可查看、编辑和版本化，但必须明确“当前不可直接运行”；关键目标依赖尚不存在的能力时停止并说明缺口，不能自动换成 Web 实现。
- **当前实现：** Web 源码、iframe Preview 和 Public Route 已实现；通用 SourceBundle 以及非 Web 构建、测试和运行适配器尚未实现，因此当前代码不能宣称已经完整交付任意技术栈。
- **演进方向：** 先把项目类型、源码包和 Runtime Adapter Contract 从 Web AppSpec 中拆开，再按实际需求增加受控适配器；不直接开放任意 Shell、网络、Secret 或依赖安装。

## 5. 版本如何服务整体目标

- **[V1：证明完整闭环]** 用固定专业团队、项目代码仓库、版本和显式发布证明持续交付闭环；Web Runtime 是当前首个已实现适配器，不是产品类型上限。
- **[V2：提升协作能力]** 在不破坏 Project、代码归属和发布控制的前提下，引入动态任务图、角色子集、局部并行、Tool、返工和仲裁。
- **[后续：走向在线产品平台]** 按用户价值逐步增加后端、数据、第三方服务、协作、分享和增长能力；具体归属由后续版本文档决定。

V1、V2 是实现阶段，不是两个不同产品。任何版本都必须服务同一个长期闭环，不能为了展示新 Agent 或新技术而割裂 Project 历史、代码归属和用户控制。

## 6. 判断产品是否成立

- **[目标保持]** 最终结果是否保留用户原始产品目标和已确认范围。
- **[首次可用]** 用户能否在合理时间内从想法得到可交互结果，而不是停在计划或代码文本。
- **[修改成功]** 用户能否通过对话、视觉编辑或代码文件完成修改，并形成可恢复版本。
- **[过程可理解]** 系统暂停或失败时，用户是否知道当前阶段、原因、影响和下一步。
- **[代码可掌控]** 用户是否能查看、管理和带走项目代码，版本是否能追溯。
- **[发布可控制]** 线上版本是否只在用户明确操作后变化，历史版本能否恢复。
- **[项目可继续]** 用户再次进入项目时，是否能基于已有需求、代码和版本继续推进，而不是重新开始。

产品成功的最终标准不是生成了多少页面、调用了多少 Agent，而是：**用户是否真正得到并持续掌控了一个可以运行的软件项目。**
